streamlit>=1.36.0
plotly>=5.18.0
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
openpyxl>=3.1.0
requests>=2.28.0
python-dotenv>=1.0.0

# Yahoo portfolio sync (browser-use + Gemini) — install when ready:
#   pip install browser-use langchain-google-genai
#   playwright install chromium

# AI recommendations (ai-hedge-fund) — install separately:
#   git clone https://github.com/virattt/ai-hedge-fund ~/ai-hedge-fund
#   cd ~/ai-hedge-fund && poetry install
